export function DashboardPage() {
  return (
    <section className="card">
      <h2>System Dashboard</h2>
      <p>The backend modules are connected: auth, user, role, permission, and logs.</p>
      <p>Use navigation to manage resources and review operation trails.</p>
    </section>
  );
}
